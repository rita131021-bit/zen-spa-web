kind = "web"
previewPath = "/"
title = "Zen Pet Spa - Home"
version = "1.0.0"
id = "artifacts/zen-home"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25764

[services.development]
run = "pnpm --filter @workspace/zen-home run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/zen-home", "run", "build" ]
publicDir = "artifacts/zen-home/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25764"
BASE_PATH = "/"
